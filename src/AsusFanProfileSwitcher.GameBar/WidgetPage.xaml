<Page
    x:Class="AsusFanProfileSwitcher.GameBar.WidgetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Margin="20,16,20,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock
                    FontFamily="Segoe UI Semibold"
                    FontSize="19"
                    Foreground="White"
                    Text="FAN XPERT CONTROL" />
                <TextBlock
                    Margin="0,3,0,0"
                    FontSize="11"
                    Foreground="{StaticResource MutedBrush}"
                    Text="QUICK PROFILES" />
            </StackPanel>
            <Button
                x:Name="RefreshButton"
                Grid.Column="1"
                Padding="12,7"
                Click="RefreshButton_Click"
                Content="Refresh" />
        </Grid>

        <Border
            Grid.Row="1"
            Margin="20,6,20,10"
            Padding="12,9"
            Background="{StaticResource CardBrush}">
            <TextBlock
                x:Name="ConnectionText"
                Foreground="{StaticResource MutedBrush}"
                Text="CONNECTING TO CONTROLLER…"
                TextWrapping="Wrap" />
        </Border>

        <ScrollViewer Grid.Row="2" Padding="20,0,20,0">
            <StackPanel x:Name="ProfilesPanel" />
        </ScrollViewer>

        <TextBlock
            x:Name="ResultText"
            Grid.Row="3"
            Margin="20,8,20,14"
            Foreground="{StaticResource MutedBrush}"
            Text="The desktop controller must remain open."
            TextWrapping="Wrap" />

        <ProgressRing
            x:Name="BusyIndicator"
            Grid.RowSpan="4"
            Width="42"
            Height="42"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="{StaticResource AccentBrush}"
            IsActive="False" />
    </Grid>
</Page>
